#!/usr/bin/env python3
"""Convert four safety fragments into source-backed placement and response checks."""
from __future__ import annotations
import hashlib,json,sys,tempfile
from pathlib import Path
ROOT=Path(__file__).resolve().parents[3];DATA=ROOT/"data";V324=DATA/"manual_reviews/context_merit_audit_v324";V290=DATA/"manual_reviews/context_merit_audit_v290";sys.path[:0]=[str(ROOT),str(V324),str(V290)]
import build_context_merit_audit_v324 as previous
import build_context_merit_audit_v290 as core
OUT_DIR=Path(__file__).resolve().parent;AUDIT=OUT_DIR/"context_merit_audit_v325.jsonl";CURATION=OUT_DIR/"pending_curation_context_merit_v325.jsonl";REPORT=OUT_DIR/"report_context_merit_v325.json";BASELINE_ROWS=537;BASELINE_SHA256="30a15ead9fc07c4e087d545a780eb5e72904de6b0ebbe72b276e7936df5889ac";EXPECTED_OUTPUT_SHA256="e6c6d73fe1638267308b87dfd6966b9f4067f852983af4a199386413656c2826"
EXPECTED_CAPACITY_BEFORE={"conflict_units":260,"equipment_material":23,"resources_general":84,"safety_consent":83,"technique":70};EXPECTED_CAPACITY_AFTER={"conflict_units":260,"equipment_material":23,"resources_general":84,"safety_consent":82,"technique":71}
RESOURCE_MANIFEST=previous.RESOURCE_MANIFEST;file_sha256=previous.file_sha256;text_sha256=previous.text_sha256;read_jsonl=previous.read_jsonl;write_jsonl=previous.write_jsonl;conservative_capacity=previous.conservative_capacity;portable=previous.portable;PRIOR=(V324/"context_merit_audit_v324.jsonl",V324/"pending_curation_context_merit_v324.jsonl",V324/"report_context_merit_v324.json")
SPECS=(
 {"fact_id":"fact-feee71c1c51dc1fb3d79","active_index":175,"expected_question":"What compression warning does Rope365 give when wrapping a leg while it is straight?","expected_answer":"when wrapping the leg straight, folding it afterward may cause compression on the limb","question":"Why should rope fit be rechecked if a wrapped straight leg is later folded?","answer":"Folding the leg after wrapping it straight can compress the limb, so the fit in the straight position should not be assumed to remain unchanged.","reason_code":"convert_leg_compression_fragment_to_position_change_check","reason":"The replacement turns a dependent-clause fragment into the evidence's practical warning that a position change can alter limb compression."},
 {"fact_id":"fact-8110e02a7ff5c601db51","active_index":223,"expected_question":"What does Rope365 emphasize about communication and consent in pressure-point play?","expected_answer":"Communication is key and consent is particularly important with this kind of play.","question":"Why does Rope365 recommend testing intended pressure spots with each partner before combining them with rope?","answer":"A spot may tickle one person, do nothing for another, or be extremely painful for someone else, so test intended spots first, learn that partner's responses, communicate, and confirm consent.","reason_code":"replace_generic_pressure_point_consent_slogan_with_partner_check","reason":"The replacement preserves the consent guidance while restoring the source's concrete reason for partner-specific testing: responses to the same spot vary widely."},
 {"fact_id":"fact-eecc86b4b0d2110095ce","active_index":432,"expected_question":"Which deficiencies can make a common bowline unsuitable for safety-critical use?","expected_answer":"to work loose when not under load (or under cyclic loading), to slip when pulled sideways, and the bight portion of the knot to capsize in certain circumstances","question":"Which failure modes can make a common bowline unsuitable for safety-critical use?","answer":"It can work loose without steady load or under cyclic loading, slip under a sideways pull, and have its bight capsize in some circumstances.","reason_code":"repair_bowline_failure_mode_infinitive_fragment","reason":"The replacement converts the copied infinitive list into a grammatical, self-contained account of the same three failure modes."},
 {"fact_id":"fact-8c68eeca0f52263fd2a9","active_index":505,"expected_question":"Why does Rope365 say the Open V chest harness can be more comfortable?","expected_answer":"it moves the rope away from the clavicles and makes an angle that creates more natural pressure on the body","question":"What placement change does Rope365 say may make the Open V chest harness more comfortable?","answer":"Opening the front stem moves rope away from the clavicles and changes the angle at which it presses on the body, which the page says can feel more comfortable.","reason_code":"replace_natural_pressure_phrase_with_observable_open_v_placement","reason":"The replacement preserves the source's qualified comfort claim while expressing the observable rope-placement and pressure-angle changes instead of the vague phrase 'natural pressure'."},
)
def build_baseline(out,report):
 previous.build_projection(out,report)
 if (len(read_jsonl(out)),file_sha256(out))!=(BASELINE_ROWS,BASELINE_SHA256):raise ValueError("v324 drift")
def build_projection(out,report):
 d=out.parent/f".{out.name}.v325-input";d.mkdir(parents=True,exist_ok=True);base=d/"v324.jsonl";build_baseline(base,d/"v324.report.json");core.build_projection_with_inputs(out,report,(CURATION,),(base,))
def observe(before):
 with tempfile.TemporaryDirectory(prefix=".v325-observe-",dir=OUT_DIR) as t:
  d=Path(t);out=d/"out.jsonl";rep=d/"out.report.json";ds=[];rs=[]
  for _ in (1,2):build_projection(out,rep);ds.append(out.read_bytes());rs.append(rep.read_bytes())
  rows=read_jsonl(out);return{"rows":len(rows),"sha":hashlib.sha256(ds[0]).hexdigest(),"eval":json.loads(rs[0])["eval_fact_count"],"de":ds[0]==ds[1],"re":rs[0]==rs[1],"before":conservative_capacity(before),"after":conservative_capacity(rows)}
def main():
 OUT_DIR.mkdir(parents=True,exist_ok=True)
 with tempfile.TemporaryDirectory(prefix=".v325-base-",dir=OUT_DIR) as t:d=Path(t);base=d/"v324.jsonl";build_baseline(base,d/"v324.report.json");before=read_jsonl(base)
 by_fact={r["fact_id"]:(i,r) for i,r in enumerate(before,1)};audits=[];curations=[]
 for audit_index,s in enumerate(SPECS,1):
  index,active=by_fact[s["fact_id"]]
  if index!=s["active_index"] or (active["question"],active["answer"])!=(s["expected_question"],s["expected_answer"]):raise ValueError(f"candidate drift {s['fact_id']}")
  evidence=active.get("evidence")
  if not evidence:raise ValueError("missing evidence")
  curations.append({"action":"edit","answer":s["answer"],"document_sha256":active["document_sha256"],"evidence":evidence,"evidence_url":active["url"],"expected_answer":active["answer"],"expected_question":active["question"],"fact_id":active["fact_id"],"paraphrase_rationale":s["reason"],"question":s["question"],"reason":s["reason"],"reason_code":s["reason_code"],"reviewed_at":"2026-07-15","reviewer":"codex-context-merit-audit-v325","source_lineage":active["source_lineage"],"support_type":"manual_paraphrase"})
  audits.append({"active_answer":active["answer"],"active_index":index,"active_question":active["question"],"audit_index":audit_index,"decision":"edit","document_sha256":active["document_sha256"],"edited_answer":s["answer"],"edited_question":s["question"],"fact_id":active["fact_id"],"paraphrase_rationale":s["reason"],"projection_lineage":{"active_index":index,"baseline_rows":BASELINE_ROWS,"baseline_sha256":BASELINE_SHA256},"reason":s["reason"],"reason_code":s["reason_code"],"review_pass":"observable_placement_and_partner_response_train_only_cleanup","reviewed_at":"2026-07-15","reviewer":"codex-context-merit-audit-v325","schema":"context-merit-audit-v325","source":active["source"],"source_support":"manual_source_and_dataset_context_review","support_evidence":evidence,"support_evidence_sha256":text_sha256(evidence),"url":active["url"]})
 write_jsonl(AUDIT,audits);write_jsonl(CURATION,curations);o=observe(before)
 if not o["de"] or not o["re"] or (o["rows"],o["eval"])!=(537,612) or o["before"]!=EXPECTED_CAPACITY_BEFORE:raise ValueError(f"projection drift {o}")
 if EXPECTED_CAPACITY_AFTER!="PENDING" and o["after"]!=EXPECTED_CAPACITY_AFTER:raise ValueError(f"capacity drift {o}")
 if EXPECTED_OUTPUT_SHA256!="PENDING" and o["sha"]!=EXPECTED_OUTPUT_SHA256:raise ValueError("output drift")
 REPORT.write_text(json.dumps({"audit":{"by_decision":{"drop":0,"edit":4,"keep":0},"path":portable(AUDIT),"rows":4,"sha256":file_sha256(AUDIT)},"conservative_capacity":{"after":o["after"],"before":o["before"],"delta":{k:o["after"][k]-o["before"][k] for k in o["before"]},"grouping":"shared document SHA, normalized URL, raw lineage family, or pinned v13 lexical-semantic cluster"},"prior_checkpoint":{"candidate":{"rows":BASELINE_ROWS,"sha256":BASELINE_SHA256},"artifacts":[{"path":portable(p),"sha256":file_sha256(p)} for p in PRIOR]},"isolated_build_projection":{"automated_projection_runs":2,"new_additions_applied":0,"output_rows":o["rows"],"output_sha256":o["sha"],"repeat_dataset_byte_identical":o["de"],"repeat_projection_report_byte_identical":o["re"],"sealed_eval_fact_count_reported_by_tooling":o["eval"]},"new_pending_curation":{"decisions":4,"path":portable(CURATION),"sha256":file_sha256(CURATION)},"schema":"context-merit-audit-report-v325","sealed_evaluation_policy":{"automated_collision_tool_reads_sealed_content":True,"automated_read_scope":"fact-ID collision exclusion and aggregate eval_fact_count reporting only","manual_worker_opened_eval_or_heldout_content":False,"manual_worker_received_eval_or_heldout_content":False}},ensure_ascii=False,indent=2,sort_keys=True)+"\n")
if __name__=="__main__":main()
